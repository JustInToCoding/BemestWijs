{{#percelen-wrapper as |percelen perceelGegevens colors selected setStep|}}
  {{test-map percelen=percelen colors=colors selected=selected}}
  {{#paper-stepper currentStep=currentStep onStepChange=(action "changeStep" setStep) vertical=true linear=false as |stepper|}}
    {{#stepper.step label="Eerste keer drijfmest" as |step|}}
      {{#step.body}}
        {{percelen-table percelen=perceelGegevens}}
      {{/step.body}}
    {{/stepper.step}}
    {{#stepper.step label="Kunstmest strooien" as |step|}}
      {{#step.body}}
        {{!-- Content here. Probably some form. --}}
        {{percelen-table percelen=perceelGegevens}}
      {{/step.body}}
    {{/stepper.step}}
    {{#stepper.step label="Maisland prepareren" as |step|}}
      {{#step.body}}
        {{!-- Content here. Probably some form. --}}
        {{percelen-table percelen=perceelGegevens}}
      {{/step.body}}
    {{/stepper.step}}
    {{#stepper.step label="Tweede snede bemesten" as |step|}}
      {{#step.body}}
        {{!-- Content here. Probably some form. --}}
        {{percelen-table percelen=perceelGegevens}}
      {{/step.body}}
    {{/stepper.step}}
    {{#stepper.step label="Latere snedes bemesten" as |step|}}
      {{#step.body}}
        {{!-- Content here. Probably some form. --}}
        {{percelen-table percelen=perceelGegevens}}
      {{/step.body}}
    {{/stepper.step}}

    {{!-- add as many steps as necessary --}}

  {{/paper-stepper}}
{{/percelen-wrapper}}