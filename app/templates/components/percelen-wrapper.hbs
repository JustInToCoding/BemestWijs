{{#if (eq step -1)}}
  {{yield percelen perceelGegevens colors selected (action 'setStep')}}
{{else if (eq step 0)}}
  {{yield percelenStep0 perceelGegevens0 colors selected (action 'setStep')}}
{{else if (eq step 1)}}
  {{yield percelenStep1 perceelGegevens1 colors selected (action 'setStep')}}
{{else if (eq step 2)}}
  {{yield percelenStep2 perceelGegevens2 colors selected (action 'setStep')}}
{{else if (eq step 3)}}
  {{yield percelenStep3 perceelGegevens3 colors selected (action 'setStep')}}
{{else if (eq step 4)}}
  {{yield percelenStep4 perceelGegevens4 colors selected (action 'setStep')}}
{{/if}}